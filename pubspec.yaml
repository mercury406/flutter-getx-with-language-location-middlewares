name: flutter_map_pet
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  get_storage: ^2.0.3
  floor: ^1.3.0
  sqflite: any
  slide_countdown: ^0.2.7
  modal_bottom_sheet: ^2.0.1
  flutter_map: ^1.0.0
  latlong2: ^0.8.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  floor_generator: ^1.3.0
  build_runner: ^2.1.2

flutter:
  uses-material-design: true